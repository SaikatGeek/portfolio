# driver screenshot output
.skill-shots/

# local scratch / old experiments — not part of the site
ex files/

# secrets — .env is generated on the droplet by CI, never committed
.env

# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example
!.env.example
